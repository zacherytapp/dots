#
# ~/.config/paru/paru.conf
#
# User config. Pulls in the system config, then overrides.
# See the paru.conf(5) manpage for options.

[options]
Include = /etc/paru.conf

# Skip the PKGBUILD review prompt before building AUR packages.
SkipReview
