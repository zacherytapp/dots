# `rm` hook tests

Replaced by [`test-safety-hook.sh`](test-safety-hook.sh), which includes smoke
tests for chained `rm` and command-substitution `rm` blocking through the local
bash hook entrypoint.
